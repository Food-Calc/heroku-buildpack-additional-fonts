# heroku-buildpack-helvetica-fonts

Heroku buildpack for installing Helvetica fonts on an heroku instance which as of heroku-16 doesn't seem to include, the fonts are installed under the user's `.fonts` dir.
